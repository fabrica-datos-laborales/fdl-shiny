library(shiny)
library(bs4Dash) # remotes::install_github("RinteRface/bs4Dash", force = TRUE)

library(dplyr)
library(highcharter)

# parametros
PARS <- list(
  # color pricipal, main color
  color_main = "#EC1E28"
)
